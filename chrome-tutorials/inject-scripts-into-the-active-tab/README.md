Hide all sections on Google results page and display its header
- Sections indicated with `data-rpos` attribute
- "People also search for" section has `data-abe` 
- Headers found as `span` with role of `heading`

More pronounced results
- Larger web page badge (first `img` with `data-atf`)
- Hide result image (following `img` with `data-atf` and `alt`)
