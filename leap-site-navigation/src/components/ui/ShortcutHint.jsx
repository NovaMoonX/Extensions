import React from 'react';

export default function ShortcutHint() {
  return (
    <div className="shortcut-hint">
      Tip: Press <kbd>Option+L</kbd> to quickly add or open current page · <kbd>Option+Shift+M</kbd> for Leaflets
    </div>
  );
}
